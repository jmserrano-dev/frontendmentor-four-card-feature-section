export interface ThemeModel {
  colors: {
    primary: {
      red: string;
      cyan: string;
      orange: string;
      blue: string;
    };
    neural: {
      veryDarkBlue: string;
      grayishBlue: string;
      veryLightGray: string;
    };
  };
}
